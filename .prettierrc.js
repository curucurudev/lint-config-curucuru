/** @type {import('prettier').Config} */
const config = {
    printWidth: 120,
    tabWidth: 4,
    useTabs: false,
    singleQuote: true,
    proseWrap: 'preserve',
    trailingComma: 'all',
    semi: true,
};
module.exports = config;
